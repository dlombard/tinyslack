import React, {PureComponent} from 'react'
import styles from './styles.scss'

export default class SideNavigation extends PureComponent {
  render() {
    return <nav className={styles.navigation}>Side Navigation</nav>
  }
}
